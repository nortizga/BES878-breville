<!DOCTYPE html>
<html lang="en">
{%- set pageTitle %}{{ meta.siteName }}{% endset -%}
{%- set pageDescription %}{{ meta.siteDescription }}{% endset -%}
<head>
{% include 'head.njk' %}
</head>
<body>
    <main class="container">
        {% include 'sitenav.njk' %}
        <h1>{{ description.title }}</h1>
        <small class="subheading">{{ description.subtitle }}</small>
        {{ content | safe }}
    </main>
    {% include 'footer.njk' %} 
</body>
</html>